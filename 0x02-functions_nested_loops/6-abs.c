#include "main.h"

/**
 * _abs - prints absolute value of an integer
 * @abs: integer to check
 * Return: absolute value of set
 */
int _abs(int abs)
{
	if (abs < 0)
	abs = abs * -1
		return (abs);
}
