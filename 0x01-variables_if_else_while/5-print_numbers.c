#include <stdio.h>
/**
 * main - Lists digits of base '10'
 * Description: Only single digits
 * Return: Always 0 (Success)
 */

int main(void)
{
	int digits;

	for (digits = 0; digits < 10; digits++)

	{
		printf("%d", digits);
	}

	printf("\n");

	return (0);
}

